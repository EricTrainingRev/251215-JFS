import { PokeTextConverterPipe } from './poke-text-converter-pipe';

describe('PokeTextConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeTextConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
